{
  "name": "@haiper/colors",
  "version": "0.1.3",
  "description": "Haiper colors",
  "main": "color.js",
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  },
  "scripts": {
    "lint": "standard --fix",
    "postversion": "git push --follow-tags && npm publish",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Yudu Ban <banyudu@gmail.com>",
  "license": "ISC",
  "devDependencies": {
    "standard": "^17.1.0"
  }
}
